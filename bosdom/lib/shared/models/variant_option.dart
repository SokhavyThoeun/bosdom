import 'package:flutter/material.dart';

/// A single selectable color swatch for a product/co-buy variant picker.
class ProductColorOption {
  const ProductColorOption(this.name, this.color);

  final String name;
  final Color color;
}
