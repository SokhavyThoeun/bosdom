import 'package:flutter/material.dart';

/// Bosdom brand palette — muted crimson, B2B wholesale marketplace.
abstract final class AppColors {
  // Brand core
  static const Color brandCrimson = Color(0xFF9B2C2C);
  static const Color deepBurgundy = Color(0xFF5C1717);
  static const Color roseMist = Color(0xFFC07878);

  // Tints & surfaces
  static const Color blushSurface = Color(0xFFF5E2E2);
  static const Color petalWhite = Color(0xFFFDF6F6);
  static const Color roseDivider = Color(0xFFDEC8C8);

  // Neutral text
  static const Color warmBlack = Color(0xFF1A0C0C);
  static const Color warmTaupe = Color(0xFF684848);

  // Status
  static const Color trustGreen = Color(0xFF3B6E50);
  static const Color alertAmber = Color(0xFFB07838);
}
