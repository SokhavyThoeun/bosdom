import 'package:flutter/material.dart';

import '../../../shared/widgets/widgets.dart';

class CoBuyingScreen extends StatelessWidget {
  const CoBuyingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppPlaceholderScreen(
      title: 'Co-buy pool',
      subtitle: 'Invite link & volume threshold progress — UI comes in phase 5',
    );
  }
}
